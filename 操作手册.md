# MediaCrawler 操作手册

> 适用版本：本项目（Windows + Python 3.11 + WebUI）
> 最后更新：2026-08-07

---

## 目录

1. [项目简介](#1-项目简介)
2. [环境准备与安装](#2-环境准备与安装)
3. [启动 WebUI](#3-启动-webui)
4. [WebUI 操作指南](#4-webui-操作指南)
5. [各平台 ID 获取方式](#5-各平台-id-获取方式)
6. [命令行模式](#6-命令行模式)
7. [数据存储与导出](#7-数据存储与导出)
8. [核心配置说明](#8-核心配置说明)
9. [常见问题 FAQ](#9-常见问题-faq)

---

## 1. 项目简介

MediaCrawler 是一个多平台媒体内容爬虫工具，支持以下平台与能力：

| 平台 | 代码 | 说明 |
|---|---|---|
| 小红书 | `xhs` | 笔记搜索 / 详情 / 创作者 |
| 抖音 | `dy` | 视频搜索 / 详情 / 创作者 |
| 快手 | `ks` | 视频搜索 / 详情 / 创作者 |
| B站 | `bili` | 视频搜索 / 详情 / UP主 |
| 微博 | `wb` | 微博搜索 / 详情 / 创作者 |
| 百度贴吧 | `tieba` | 帖子搜索 / 详情 |
| 知乎 | `zhihu` | 内容搜索 / 详情 |

**三种爬取模式：**

- **搜索模式（search）**：按关键词搜索该平台的内容列表
- **详情模式（detail）**：输入指定帖子/视频的 ID 或 URL，抓取指定内容及其评论
- **创作者模式（creator）**：输入创作者（UP主/博主）主页 ID，抓取其发布的内容

**注意：本项目仅供学习和研究使用，请遵守各平台使用条款，合理控制请求频率。**

---

## 2. 环境准备与安装

### 2.1 系统要求

- Python 3.11+
- Node.js 16+（仅 WebUI 前端重新构建时需要）
- 稳定的网络环境（部分平台可能需要代理）

### 2.2 安装依赖

推荐使用项目自带的虚拟环境（本项目已存在 `.venv`）：

```powershell
# 方式一：使用 uv（推荐）
uv sync

# 方式二：使用 pip
pip install -r requirements.txt
```

### 2.3 安装浏览器内核

爬虫依赖 Playwright 驱动真实浏览器：

```powershell
python -m playwright install chromium
```

### 2.4 WebUI 前端构建（仅修改前端后需要）

项目 `api/webui/` 已包含构建好的前端产物，直接启动即可。若修改了 `webui/src/` 下的代码，需重新构建：

```powershell
cd webui
npm install
npm run build
# 构建产物会自动同步到 api/webui/
```

---

## 3. 启动 WebUI

### 3.1 启动命令

```powershell
# 使用项目虚拟环境中的 Python（推荐，可避免 uv 中间层问题）
.\.venv\Scripts\python.exe -m uvicorn api.main:app --host 0.0.0.0 --port 8080

# 或使用 uv
uv run uvicorn api.main:app --host 0.0.0.0 --port 8080

# 或直接运行入口文件
python -m api.main
```

### 3.2 访问

浏览器打开：**http://localhost:8080**

> 若 8080 端口被占用，可通过 `--port` 参数更换端口。

### 3.3 停止服务

- 在运行 WebUI 的终端按 `Ctrl+C`
- 爬虫任务本身可在 WebUI 界面点击"终止"按钮停止

---

## 4. WebUI 操作指南

WebUI 主界面左侧为功能导航，包含：**配置面板、运行日志（终端）、数据浏览、运行结果**。

### 4.1 目标配置

| 配置项 | 说明 |
|---|---|
| 平台 | 选择要爬取的平台（xhs/dy/ks/bili/wb/tieba/zhihu） |
| 爬取类型 | 搜索模式 / 详情模式 / 创作者模式 |
| 起始页 | 从第几页开始爬取（搜索模式生效） |
| 关键词 | 搜索模式下输入关键词，回车添加，可添加多个（逗号分隔） |
| 帖子/视频 ID | 详情模式下输入 ID 或完整 URL（见[第 5 节](#5-各平台-id-获取方式)） |
| 创作者 ID | 创作者模式下输入创作者主页 ID 或 URL |

### 4.2 登录配置

| 配置项 | 说明 |
|---|---|
| 登录方式 | 扫码登录（qrcode）/ Cookie 登录（cookie） |
| Cookies | 选择 Cookie 登录时粘贴 Cookie 字符串 |

- **首次使用**建议选择扫码登录，浏览器窗口会弹出二维码，扫码后登录态保存至 `browser_data/` 目录，后续自动复用
- 小红书和抖音**不建议使用 Cookie 登录**（易触发滑块验证）
- 小红书 URL 必须包含 `xsec_token` 参数，请从浏览器复制完整 URL

### 4.3 输出配置

| 配置项 | 说明 |
|---|---|
| 保存格式 | jsonl / json / csv / excel / sqlite / db(MySQL) / mongodb |
| 评论抓取 | 勾选后抓取一级评论 |
| 子评论 | 勾选后递归抓取评论下的二级评论（楼中楼），请求量大、耗时较长 |
| 无头模式 | 浏览器无界面运行（Headless），后台执行 |

### 4.4 运行与日志

- 点击 **开始爬虫** 按钮启动任务
- 运行期间可实时查看日志终端（WebSocket 推送）
- 点击 **终止** 可优雅停止任务，已爬取数据会自动落盘并导出

### 4.5 数据查看与导出

- **数据浏览**：可查看 `data/` 目录下的数据文件，支持预览（JSON/CSV/Excel）与下载
- **运行结果**：任务结束后展示本次任务文件；任务数据会自动复制到 `exports/` 目录留存
- **文件导出**：可将本次任务数据打包为 zip 下载

---

## 5. 各平台 ID 获取方式

### 5.1 B站（bili）

| 类型 | 示例 |
|---|---|
| 视频 ID | `BV1xxxx` 或 `https://www.bilibili.com/video/BV1xxxx` |
| UP主 ID | `434377496` 或 `https://space.bilibili.com/434377496` |

### 5.2 小红书（xhs）

| 类型 | 示例 |
|---|---|
| 笔记 ID | `https://www.xiaohongshu.com/explore/xxx?xsec_token=xxx`（必须含 xsec_token） |
| 用户 ID | `https://www.xiaohongshu.com/user/profile/xxx?xsec_token=xxx`（必须含 xsec_token） |

### 5.3 抖音（dy）

| 类型 | 示例 |
|---|---|
| 视频 ID | `7525538910311632128` 或 `https://www.douyin.com/video/xxx` 或短链接 `https://v.douyin.com/xxx` |
| 用户 ID | `MS4wLjABAAAAxxx` 或 `https://www.douyin.com/user/MS4wLjABAAAAxxx` |

### 5.4 微博（wb）

| 类型 | 示例 |
|---|---|
| 微博 ID | `4982041758140155` 或 `https://weibo.com/xxx/xxx` |
| 用户 ID | `5533390220` 或 `https://weibo.com/u/5533390220` |

### 5.5 快手（ks）

| 类型 | 示例 |
|---|---|
| 视频 ID | `3xf8enb8dbj6uig` 或 `https://www.kuaishou.com/short-video/xxx` |
| 用户 ID | `3x84qugg4ch9zhs` 或 `https://www.kuaishou.com/profile/xxx` |

---

## 6. 命令行模式

不使用 WebUI 时，可通过命令行直接运行：

```powershell
# 搜索模式（B站，关键词搜索）
.\.venv\Scripts\python.exe main.py --platform bili --type search --keywords "关键词1,关键词2"

# 详情模式（B站，指定视频）
.\.venv\Scripts\python.exe main.py --platform bili --type detail --specified_id "BV1xxxx"

# 创作者模式（B站，UP主）
.\.venv\Scripts\python.exe main.py --platform bili --type creator --creator_id "434377496"

# 常用参数组合示例（B站视频详情 + 抓评论 + 子评论 + 存 Excel）
.\.venv\Scripts\python.exe main.py --platform bili --type detail --specified_id "BV1xxxx" --get_comment true --get_sub_comment true --save_data_option excel
```

**常用命令行参数：**

| 参数 | 说明 |
|---|---|
| `--platform` | 平台：xhs / dy / ks / bili / wb / tieba / zhihu |
| `--type` | 类型：search / detail / creator |
| `--keywords` | 搜索关键词（逗号分隔） |
| `--specified_id` | 详情模式的 ID/URL（逗号分隔） |
| `--creator_id` | 创作者 ID（逗号分隔） |
| `--start` | 起始页 |
| `--get_comment` | 是否抓一级评论（true/false） |
| `--get_sub_comment` | 是否抓二级评论（true/false） |
| `--max_comments_count_singlenotes` | 每个视频/帖子的最大一级评论数 |
| `--crawler_max_notes_count` | 最大内容条数 |
| `--save_data_option` | 保存格式：jsonl/json/csv/excel/sqlite/db/mongodb |
| `--lt` | 登录方式：qrcode / cookie / phone |
| `--headless` | 无头模式（true/false） |
| `--login` | 只执行登录并退出 |

---

## 7. 数据存储与导出

### 7.1 数据目录结构

```
data/
├── bilibili/
│   ├── bilibili_detail_20260807_090002.xlsx   # 按"平台_类型_时间戳"命名
│   └── jsonl/
├── xhs/
├── douyin/
└── ...
exports/          # 任务结束后自动导出的数据副本
browser_data/     # 浏览器登录态（勿删除，删除需重新扫码）
```

### 7.2 Excel 输出说明

以 B站视频详情为例，Excel 包含两个工作表：

- **Contents**：视频基本信息（标题、作者、播放量、点赞等）
- **Comments**：评论数据（含 `comment_id`、`parent_comment_id`、内容、点赞数等）

> `parent_comment_id` 为 `0` 表示一级评论，非 0 表示某条一级评论的二级评论。

### 7.3 评论数量说明（重要）

**B站页面显示的"评论总数"包含二级评论（楼中楼）**，而爬虫默认只抓一级评论，因此数量会少于页面显示值。如需完整评论：

1. WebUI 勾选"子评论"选项
2. 建议在"最大评论数"输入框设置足够大的值（如 `1000`），否则一级评论会被默认上限（当前为 1000）截断

---

## 8. 核心配置说明

配置文件位于 `config/base_config.py`，常用项如下：

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `KEYWORDS` | `""` | 默认搜索关键词（WebUI 传参可覆盖） |
| `LOGIN_TYPE` | `"qrcode"` | 默认登录方式 |
| `CRAWLER_TYPE` | `"search"` | 默认爬取类型 |
| `SAVE_DATA_OPTION` | `"jsonl"` | 默认保存格式 |
| `ENABLE_GET_COMMENTS` | `True` | 是否抓一级评论 |
| `ENABLE_GET_SUB_COMMENTS` | `False` | 是否抓二级评论 |
| `CRAWLER_MAX_COMMENTS_COUNT_SINGLENOTES` | `1000` | 每个视频/帖子的一级评论上限 |
| `HEADLESS` | `False` | 浏览器无头模式 |

各平台专属配置见 `config/bilibili_config.py`、`config/xhs_config.py` 等文件。

---

## 9. 常见问题 FAQ

### Q1：评论数量比页面显示的少？

页面显示的评论总数**包含二级评论**。只勾选"评论抓取"时仅抓一级评论；勾选"子评论"后才会抓楼中楼。同时确认"最大评论数"设置足够大。

### Q2：爬取中途停止 / 报错 code=-352 / 412？

多为触发平台风控。建议：
- 降低爬取频率（配置中的 `CRAWLER_REQUEST_INTERVAL` 等参数调大）
- 保证登录态有效
- 少量多次运行，避免并发过大

### Q3：`uv` 命令报错（trampoline 脚本损坏）？

使用项目虚拟环境中的 Python 直接运行，绕过 uv：

```powershell
.\.venv\Scripts\python.exe -m uvicorn api.main:app --host 0.0.0.0 --port 8080
```

### Q4：登录态失效？

删除 `browser_data/` 目录后重新运行，扫码登录即可。

### Q5：如何只更新前端？

修改 `webui/src/` 后执行 `cd webui && npm run build`，产物自动同步到 `api/webui/`，重启 WebUI 生效。

---

*本手册基于当前项目实际运行情况编写。项目详情与更多文档请参考 `docs/` 目录及项目 README。*
